#include <stdio.h>

/**
* main - prints first 50 Fibonacci numbers, starting with 1 and 2,
*        separated by a comma followed by a space.
* Return: Always 0.
*/
int main(void){
         Int count,
         Unsigned long fib1 = 0, fib2 = q, sum;

         for (count = 0; count<50; count ++)
         {
                 Sum = fib1 + fib2;
                 print (“%lu”, sum);

                 fib1 = fib2;
                 fib2 = sum

                 if (count == 49)
                         print(“\n”);
                 Else
                         print(“,”);
         {

          Return (0);
}
